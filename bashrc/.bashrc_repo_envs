REPO_ROOT=${HOME}/Documents/Repos

function g () {
    cd ${REPO_ROOT}/$1
    source env.sh
}

_repo_complete() {
    local cur=${COMP_WORDS[COMP_CWORD]}
    COMPREPLY=( $(compgen -W "`/bin/ls ${REPO_ROOT}`" -- $cur) )
}

complete -F _repo_complete g
